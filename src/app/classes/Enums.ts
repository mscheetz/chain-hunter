
export enum AccountType {
    Basic,
    Advanced,
    Trial,
    Admin
}

export enum Interval {
    Second,
    Minute,
    Hour,
    Day
}

export enum ResultType {
    none,
    address,
    contract,
    transaction
}

export enum Severity {
    success,
    info,
    warn,
    error,
    custom
}